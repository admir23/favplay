<%= form_for @playlist do |f| %>
<%= f.label :name %>
<%= f.text_field :name, class:'form-control' %>
<br>
<%= f.submit params[:action] == 'new' ? 'Save' : 'Update', class: 'btn btn-block btn-default' %>
<% end %>