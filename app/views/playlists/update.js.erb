$("#myupdateplaylist_<%= @playlist.id %>").modal('hide');

$("#playlist_<%= @playlist.id %>").fadeOut(500, function(){

	$(this).remove();
	$("#container_playlists").prepend('<%= j render @playlist %>');
	
});
