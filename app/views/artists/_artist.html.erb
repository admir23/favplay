<div class="col-sm-6 col-md-4 col-lg-3 mt-4">
	<div class="card">
	 <%= link_to artist_path(artist) do %>
	   <%=image_tag artist.artist_image, class: "card-img-top" %>
	 <% end %>   
		<div class="card-block">
		  <p><%= link_to artist.name, artist_path(artist) %></p>
		  <br>
		  <% if artist.user_id == current_user.id || current_user.superadmin? %>
		  <%= link_to 'Edit', edit_artist_path(artist), class:'btn btn-sm btn-secondary'%>
		  <%= link_to 'Delete', artist_path(artist), method: :delete,data: {confirm: "Are you sure?"}, class:'btn btn-sm btn-danger'%>
		  <% end %>
		</div>
	</div>
</div>